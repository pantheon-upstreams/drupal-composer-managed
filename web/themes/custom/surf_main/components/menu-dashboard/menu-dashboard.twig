{#
/**
 * @file
 * Component for Menu - Dashboard
 * This component is simplified to allow for the entire menu object to be placed
 * with an already predefined wrapper from where this component is being placed.
 *
 * Available props:
 * - content: [object] the entire build of the menu.
 *
 */
#}

{{ content }}
