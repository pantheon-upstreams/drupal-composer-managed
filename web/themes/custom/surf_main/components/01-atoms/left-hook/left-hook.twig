{#
/**
 * @file
 * Component for Left Hook
 *
 * Available props:
 * - attributes: [Drupal\Core\Template\Attribute] Drupal reserved space for attributes.
 * - left_hook_attributes: [array] Any parent tag attributes.
 *
 * Example usage:
{% embed 'surf_main:left-hook' with {
  left_hook_attributes: create_attribute(),
} only %}
{% endembed %}
 */
#}

{% set classes = [
  'a-left-hook',
] %}

<hr {{ left_hook_attributes.addClass(classes).merge(attributes) }}>
