{%
  set classes = [
  'a-grid',
]
%}

<ul {{ grid_attributes.addClass(classes).merge(attributes) }} {% if grid_styles %}style="{{ grid_styles }}"{% endif %}>
  {% block grid_items_block %}
    {{ grid_items }}
  {% endblock %}
</ul>
