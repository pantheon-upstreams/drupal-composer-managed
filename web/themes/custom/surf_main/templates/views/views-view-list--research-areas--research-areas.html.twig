{#
/**
 * @file
 * Theme override for a view template to display a list of rows.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows for this list.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 * - list: @todo.
 *   - type: Starting tag will be either a ul or ol.
 *   - attributes: HTML attributes for the list element.
 *
 * @see template_preprocess_views_view_list()
 */
#}
{#{{ dd(_context|keys) }}#}
{% if attributes -%}
<div{{ attributes }}>
  {% endif %}
  {% if title %}
    <h3>{{ title }}</h3>
  {% endif %}

  <{{ list.type }}{{ list.attributes }}>

  {% for row in rows %}
    <li{{ row.attributes }}>
      {{- row.content -}}
{#      {% include 'surf_main:link-highlight' with {#}
{#        attributes,#}
{#        link_highlight_attributes: create_attribute(),#}
{#        link_highlight_media: row.content.field_icon|field_value|default([]),#}
{#        link_highlight_text: 'row.content.description|field_value|default([])',#}
{#        link_highlight_button_text: 'row.content',#}
{#        link_highlight_button_url: 'http://www.example.com',#}
{#        link_highlight_type: 'img-right',#}

{#      } only %}#}
    </li>
  {% endfor %}

</{{ list.type }}>

{% if attributes -%}
  </div>
{% endif %}
