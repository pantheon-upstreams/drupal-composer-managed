
{% set style = 'no_dateTimeSeparator' %}
 {% if dates and times %}
  {% set style = 'dateTimeSeparator' %}
{% endif %}

<article {{ clAttributes.addClass('c-event'|addModifiers(style)) }}>

  <header class="c-event-header__wrapper">

    {# Breadcumbs #}
    {% if breadcrumbs %}
      	 {% include "breadcrumbs" with {
					items: breadcrumbs
				} %}
    {% endif %}

    {# Title #}
    <h1 class="c-event-title">{{title}}</h1>

    {# Dates and Link #}
    {% if dates or link_url %}
    <ul class="c-event-details__wrapper">

      <div class="c-event-datetime__wrapper">

        {# Dates #}
        {% if dates %}
        <li class="c-event-dates">{{dates}}</li>
        {% endif %}

        {# Times #}
        {% if times %}
        <li class="c-event-times">{{times}}</li>
        {% endif %}

      </div>

      {# Link #}
      {% if link_url %}
      <li class="c-event-link">
        <a href="{{link_url}}" class="forest-button">
          {% if link_text %}{{ link_text }}{% else %}Visit Website{% endif %}
        </a>
      </li>
      {% endif %}
    </ul>
    {% endif %}

  </header>

  <hr class="left-hook-divider">

  <div class="c-event-content__wrapper">

    {% if image_url %}
    <div class="c-event-image__wrapper">
      <img src="{{image_url}}" alt="{{image_alt}}">
    </div>
    {% endif %}

    <div class="c-event-content__inner-wrapper">
    {# Overview #}
    {% if overview %}
    <h2 class="c-event-overview">
      {{ overview }}
    </h2>
    {% endif %}

    {# Body #}
    {% if body %}
    <div class="c-event-body__wrapper">
      {{ body }}
    </div>
    {% endif %}
    </div>

  </div>


</article>