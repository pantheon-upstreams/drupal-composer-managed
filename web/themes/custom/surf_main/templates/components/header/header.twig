<header {{ attributes.addClass(['c-header', header_text_style, header_particle_style, 'l-constrained--xlarge', 'l--bottom-spacing']).merge(clAttributes) }}>
  {% if heading and type == 'h1' %}
    <h1>{{heading}}</h1>
  {% else %}
    <h2>{{heading}}</h2>
  {% endif %}
</header>
