<section {{ clAttributes.addClass('c-accordion') }}>

  {% block accordion_block %}
  {% for key, accordion_item in items %}

  <h3 class="c-title">
    <button type="button" aria-expanded="{% if loop.first %}true{% else %}false{% endif %}" class="c-accordion-trigger"
      aria-controls="sect-{{key}}" id="accordionid-{{key}}">
      <span class="c-accordion-title__wrapper">
        <span class="c-accordion-icon"></span>
        <span class="c-accordion-title-text">{{accordion_item.title}}</span>
      </span>
    </button>
  </h3>

  <div id="sect-{{key}}" role="region" aria-labelledby="accordionid-{{key}}" class="c-accordion-panel" {% if loop.first
    %}{% else %}hidden="true" {% endif %}>
    <div class="inner">
      {{accordion_item.content|check_markup('plain')}}
    </div>
  </div>

  {% endfor %}
  {% endblock %}
</section>