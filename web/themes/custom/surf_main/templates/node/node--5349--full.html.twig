{% set title = label|field_value %}

{% set body = content.field_p_content %}

{% if content.field_layout.0 is not empty %}
  {% set layout = content.field_layout.0["#markup"] %}
{% endif %}


{% include "about-page" with {
	title,
  body,
  layout,
} %}
