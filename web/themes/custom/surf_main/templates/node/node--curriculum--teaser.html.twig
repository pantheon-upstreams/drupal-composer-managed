{# <pre>{{dump(content)}}</pre> #}

{#
  TODO: replace/update the hardcoded `category_url`
#}

{% set image_url = '/themes/custom/surf_main/templates/components/teaser/curriculum-placeholder.png' %}
{% if content.field_media_primary|render %}
  {% set image_url = file_url(content.field_media_primary['#items'].entity.field_media_image.entity.fileuri) %}
{% endif %}

 {% include "teaser" with {
  layout: 'column-stack',
  title: label,
  body: '',
  url: url,
  category: 'Curriculum',
  category_url: '/curriculum',
  teaser_date: content.field_date_published.value|date('F d, Y'),
  image: image_url,
  alt: content.field_media_primary['#items'].entity.field_media_image.alt,
  } %}