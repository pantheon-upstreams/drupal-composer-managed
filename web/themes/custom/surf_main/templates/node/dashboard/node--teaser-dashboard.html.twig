{# Variables: dashboard_url: what this is, and where it's coming from #}

{% include '@surf_main-components/teaser/teaser.twig' with {
  description: content.body,
  title: label,
  url: dashboard_url,
  attr: attributes
} %}
