{# <pre>{{dump(content)}}</pre> #}

{% set heading = content.field_heading_left.0["#context"].value|raw %}

{{ include('surf_main:homepage-header', {
  heading,
  attributes,
}, with_context = false) }}
