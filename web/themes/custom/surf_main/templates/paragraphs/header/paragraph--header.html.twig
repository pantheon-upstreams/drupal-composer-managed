{# <pre>{{dump(content.field_horizontal_rule.0["#markup"])}}</pre> #}
{% if content.field_horizontal_rule.0["#markup"] == "true" %}
  {% set heading_hr = 'horizontal-rule' %}
{% endif %}

{{ include('surf_main:header', {
  heading: content.field_title|field_value,
  heading_hr,
  heading_style: content.field_header_style.0["#markup"],
  type,
  attributes,
}, with_context = false) }}
