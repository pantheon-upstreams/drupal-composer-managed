{#
/**
 * @file
 * Default theme implementation to display a Resource Link Paragraph.
 *
 * Available variables:
 * - No additional variables are available for this Paragraph type.
 *
 * @see templates/paragraphs/paragraph.html.twig
 */
#}

{% set rendered_content = content|without(
    'field_link',
) %}

{{ content.field_link|field_value }}
{{ rendered_content }}
