{#
/**
 * @file
 * Default theme implementation to display a .
 *
 * Available variables:
 * -
 *
 */
#}

{% set classes = [
    'paragraph',
    'paragraph--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
] %}

{% set rendered_content = content|without(
    'field_p_accordion_items'
) %}

{{ rendered_content }}

{% include 'surf_main:accordion' with {
    attributes,
    accordion_attributes: create_attribute({'class': classes}),
    accordion_items: content.field_p_accordion_items|field_value,
} only %}
