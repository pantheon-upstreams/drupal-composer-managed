{# <pre>{{dump(content.field_text)}}</pre> #}

{% set heading = content.field_title.0["#context"].value %}
{% set overview = content.field_overview.0["#context"].value %}
{% set body = content.field_text|field_value %}
{% set subtitle = content.field_subtitle.0["#context"].value|raw %}
{% set image_url = file_url(content.field_media.0["#media"].field_media_image.entity.fileuri | image_style('featured_image')) %}
{% set image_alt = content.field_media.0["#media"].field_media_image.alt %}

{{ include('surf_main:about-hero', {
  heading,
  overview,
  body,
  subtitle,
  image_url,
  image_alt,
  attributes,
}, with_context = false) }}

