{% set items = [] %}
{% for item in paragraph.field_media_items %}
{% set items = items|merge([{
  id: item.value.target_id
  }]) %}
{% endfor %}

{{ include('surf_main:media-gallery', {
  gallery_items: items,
  attributes
}, with_context = false) }}