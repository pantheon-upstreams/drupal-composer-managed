{#
/**
 * @file
 * Default theme implementation to display a Heading Paragraph.
 *
 * Available variables:
 * - No additional variables are available for this Paragraph type.
 *
 * @see templates/paragraphs/paragraph.html.twig
 * @see components/01-atoms/heading/heading.twig
 *
 */
#}

{% set classes = [
    'paragraph',
    'paragraph--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished'
] %}

<div {{ attributes.addClass(classes) }}>
    {% include 'surf_main:heading' with {
        rendered_content,
        heading_attributes: create_attribute(),
        heading_text: content.field_title|field_value|default([]),
        heading_level: '2',
        heading_type: style_options['heading_type']|default('none'),
        heading_rule: style_options['add_rule']|default('0'),
    } only %}
</div>

{{ content|without(
    'field_title',
) }}
